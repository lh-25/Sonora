# Copy this to .env.local and fill in your values

# Django backend URL (no trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:8000/api

# Spotify app Client ID (from developer.spotify.com)
# The Client Secret goes ONLY on the backend server (never in the mobile app)
EXPO_PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
